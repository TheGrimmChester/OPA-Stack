# Security scan workspace (Wave 33)

Intentional fixture files for Agent-embedded lite scanners (`OPA_SECURITY_WORKSPACE=/workspace`).

Contains fake credentials, eval/innerHTML JS, unpinned Dockerfile, and a Terraform stub so
`POST /api/security/runs` smoke produces findings. Not production code.
