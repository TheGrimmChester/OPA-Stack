# Default Compose entry for laptop Open-* smoke (*:smoke only).
# Production / NAS: use compose.nas.yaml (*:nas only) — never smoke on NAS.
#
# Equivalent to: docker compose -f compose.all.yaml …
include:
  - path: compose.all.yaml
