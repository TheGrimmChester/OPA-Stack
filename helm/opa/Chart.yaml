apiVersion: v2
name: opa
description: Open Profiling Agent — optional Kubernetes packaging (HA / scale). Single-node docker compose remains the default path.
type: application
version: 0.15.0
appVersion: "15"
